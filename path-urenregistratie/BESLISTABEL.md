# Beslistabel — Path Uren & Facturatie

Vastgelegde keuzes tot en met 3 september 2026. **Doel: niet opnieuw bespreken.**
Wijkt een nieuwe wens hiervan af, dan wint de nieuwe wens — pas dan deze tabel aan.
Losse regressiestatus staat in `MASTERCHECKLIST.md`; livegang-checklist in `PRODUCTIE-CHECKLIST.md`.

## 1. Werkwijze & release

**W10 bijgewerkt op 10 september 2026:** de hieronder historisch beschreven
substringfout is opgelost. `set-version.mjs` sluit cijfers en punten direct vóór
en ná de versie uit. `0.0.1` mag weer als versietoken worden verwerkt zonder
`127.0.0.1` te raken. Het script valideert alle bestanden vóór het schrijven;
de echte CLI-regressie `set-version-check.mjs` draait mee in `version:check`.

| # | Keuze | Reden | Status |
|---|---|---|---|
| W1 | Werk rechtstreeks vanuit `main`. | Kleine solo-stroom, geen PR-overhead. | vast |
| W2 | Deploy uitsluitend naar **TEST**. De gebruiker test TEST zelf. | Gecontroleerde acceptatie vóór PROD. | vast |
| W3 | **PROD wordt alleen door de gebruiker vrijgegeven/doorgezet.** Claude raakt PROD niet aan (geen deploy, geen `Promote Prod`, geen SQL). | Onomkeerbaar en naar buiten gericht. | vast |
| W4 | Pipeline: pushen naar `main` start de Release Pipeline; Validate → Promote Test → **Deploy Test to TransIP** gebeurt automatisch. `Promote Prod` blijft handmatig. | — | vast |
| W5 | `Publish Live Docs` is een niet-blokkerende post-deploy-job (GitHub Pages staat uit). Een rode Live Docs blokkeert de TEST-deploy niet. | — | vast |
| W6 | Testmethode: volledige pass, faalgevallen verzamelen, bulk-fixen, hertesten. Elk geval hoort in een feature-bestand. | Sneller dan één-voor-één. | vast |
| W7 | CI-flake ≠ regressie. Een case die lokaal 2–3× groen is en in CI viel op een drukke runner, is flake. Voorbeeld: `TS-REV-UI-H-008` (Live Docs-run 2 sep). | — | vast |
| W8 | Geen `--no-verify`, geen forced clicks, geen timeout verhogen om een test groen te krijgen. | Vertrouwen in de suite. | vast |
| W9 | **Voormalige flakes in de volle seriële regressie** (`workers:1`, ~39 min, geen retries lokaal): `TS-REV-UI-H-008` en `ADM-WR-N-006`. Beide vielen alleen om na ~340 voorgaande tests door opgebouwde DB-state. **Opgelost in 1.0.16:** `TS-REV-UI-H-008` mockt nu `bootstrap.php` (start-datum medewerker 2 terug vóór de periode), `timesheets.php` en `customer-timesheets.php` volledig — de reviewketen-assertie hangt niet meer aan gedeelde DB-state. `ADM-WR-N-006` doet drie echte sequentiële serverwrites tegen een database met honderden opgebouwde accounts; dat is traag, niet flaky — `test.slow()` + 20s op de setup-asserties (de blokkade-assertie zelf staat nog op 10s). Dit valt niet onder W8: het resultaat klopte altijd, alleen de standaard 5s-expect was te kort voor een echte trage save. | Testhygiëne, geen app-bug. | vast |

| W11 | **Scroll sluit de topbalkmenu's, maar niet vlak na een hertekening.** `window.scroll` sloot profiel- en meldingenmenu ook bij een scroll-event dat de gebruiker niet zelf maakte: een achtergrond-hertekening (werkvoorraad die binnenkomt, laadtekst die wordt vervangen) verandert de paginahoogte en geeft zo'n event. Sinds 0.9.170 wordt een scroll binnen `LAYOUT_SCROLL_GRACE_MS` (300 ms) na `markLayoutRender()` genegeerd. Dit was al langer een wrat — `NOT-H-009` had er een eigen omweg voor — en werd zichtbaar doordat 0.9.165 een laadtoestand toevoegde. Verklaart de flakes op `DASH-H-004` en `NOT-H-009`. **1.0.20:** `refreshNotificationsReadApi` roept nu óók `markLayoutRender()` aan vóór `renderNotifications()` / `renderEmployeeAnnouncementArchive()` (die pad ontbrak), en `NOT-H-009` heropent het menu via een `toPass`-lus i.p.v. één `isHidden`-check — een dubbele achtergrond-render kon de omweg anders alsnog verslaan (viel om in de 1.0.19-pipeline, Validate-shard 3). | Een menu dat uit zichzelf dichtklapt vlak na inloggen. | vast |
| W12 | **Dekkingsronde is standaard.** Op het commando **"dekkingsronde"** (of "loop de hele app na op testdekking") loopt Claude elk scherm, elke flow en elk zichtbaar/klikbaar element van de app af, kruist dat af tegen de regressiesuite, en maakt voor elk gat een nieuwe case met **echte assertions** — desktop én mobiel (`mobile-chrome` = Android, `mobile-safari` = iOS), inclusief database-controles (opgeslagen rij, audit_log, e-mail-aflevering, geen weesrijen). Fixen tot groen, nooit rood of skipped inleveren. Volledige procedure en gebiedenlijst: **`DEKKINGSRONDE.md`**. Draaien in elk geval vóór een Promote Prod. Los hiervan geldt permanent: [[new-work-ships-with-regression-tests]] — nieuw werk gaat in dezelfde release de suite in. | De gebruiker wil niet meer handmatig hoeven testen om zeker te zijn dat het werkt. | vast |
| W10 | **Versieschema.** Op 3 sep 2026 naar `1.0.0`; op 6 sep 2026 herzien naar een `0.x`-lijn omdat `1.x` ten onrechte productie-rijp leest terwijl de herontwerp-fase loopt. **`0.0.1` bleek onbruikbaar**: `set-version.mjs` doet een kale string-replace en `0.0.1` zit ín `127.0.0.1` (loopback-IP in o.a. `smoke-test.mjs`) — dat werd stilzwijgend `127.0.0.2`. Daarom nu **`0.10.0`** en van daaruit `0.10.x` per commit (veilig token, geen substring-botsing). De **echte productieversie** (`1.0.0`) komt pas bij go-live; daarna `1.0.x` fixes, `1.1.0` functies. Nummer staat op 13 plekken in 5 bestanden; zet het nooit met de hand maar met **`npm run version:set <versie>`**. `npm run version:check` (in `npm run check`) faalt zodra ergens een ander nummer staat dan in `package.json`. **TODO:** `set-version.mjs` hardenen met een token-grens zodat een versie nooit meer binnen een IP of ander getal kan matchen. | "1.x" leest als af; naïeve string-replace botste met `127.0.0.1`. | vast |
| W13 | **HTML-mail met logo: alleen voor `password_reset` (uitnodiging + wachtwoord-reset).** Migratie 022 koos bewust tegen een opgemaakte (HTML) mail met logo voor de hele mailmodule, om de bewezen platte-tekst-verzendlaag (`server/mail/smtp.php`) niet te raken — die keuze stond alleen in de migratie zelf, niet hier. Op 5 sep 2026 is er expliciet om gevraagd voor de accountuitnodiging en wachtwoord-reset specifiek. Uitvoering: `smtp_relay_send()`/`smtp_build_message()` kregen een optionele `$htmlBody` (standaard `null`, dus voor elk ander kanaal ongewijzigd `text/plain`); alleen `auth_enqueue_password_reset()` vult een nieuwe kolom `email_deliveries.html_snapshot` (migratie 027), met dezelfde scrub-na-verzending/mislukking als `body_snapshot` (bevat dezelfde eenmalige link). Elke andere `INSERT INTO email_deliveries` (facturen, acceptatietest) laat die kolom leeg — gepind in `PWD-N-017`. Het oorspronkelijke risico (de bewezen verzendlaag raken) blijft voor facturen dus gelden; voor uitnodiging/reset is het bewust opnieuw afgewogen omdat die twee geen PDF-bijlage en geen historie van verzendproblemen hebben. | De gebruiker wilde de echte Path-huisstijl (logo, contactregels, tagline) in de uitnodigings- en reset-mail, niet alleen platte tekst. | vast |

## 2. Productie & pilot

| # | Keuze | Reden | Status |
|---|---|---|---|
| P1 | Publieke controle op 6 sep 2026: `uren.pathconsultancy.nl` is gezond en toont versie **0.9.156**. De eerstvolgende productierelease wordt pas na TEST-acceptatie **1.0.0**. | Geen versieclaims op basis van oude notities. | feit |
| P2 | De master-datamigratie naar PROD is **al gebeurd** (eenmalig). `server/scripts/migrate-test-masterdata-to-production.php` **nooit opnieuw op PROD draaien**; blijft buiten Git (gitignored). Het afgesproken nulpunt is 2 beheerders + Marc/Brian/Stasjo/Shawn + `PROD Pilot Medewerker`, met voor alle vijf medewerkers `employment_start_date = 2026-09-01` en lege operationele tabellen. De 1.0.0-deploy controleert dit read-only en stopt vóór backup/migratie als het afwijkt. Een beheerder mag daarna een startdatum eerder zetten; na opslaan volgt de maandnavigatie die gewijzigde persoonlijke startdatum. | Eenmalige nulmeting zonder toekomstige echte data te blokkeren. | vast |
| P3 | De 4 echte medewerkeraccounts (Marc, Brian, Stasjo, Shawn) blijven **gelockt** (`force_password_change`, geen wachtwoord) tot expliciete vrijgave. | Kunnen niet inloggen → kunnen niks in gang zetten. | vast |
| P4 | Productiepilotmail alleen via `configure-production-mail-pilot.php --mode=pilot`. `allowed_recipients` = **uitsluitend de dummy-sinks** (`gambitizanagi+prod-boekhouder@`, `+prod-salaris@`, broker/klant → `gambitizanagi@gmail.com`). In pilotmodus wordt elke andere ontvanger geblokkeerd. | Dubbel slot: gelockte accounts + allowlist. | vast |
| P5 | De PROD-dummy `PROD Pilot Medewerker` **niet herbouwen**. Accountadres `joycesteenhoven@gmail.com`; klant + broker `gambitizanagi@gmail.com`; pilotboekhouder `gambitizanagi+prod-boekhouder@gmail.com`; pilotsalaris `gambitizanagi+prod-salaris@gmail.com` zonder factuur-PDF. De 1.0.0-preflight verifieert deze waarden read-only. | De al ingerichte live set is bronwaarheid; geen tweede masterdata-import. | vast |
| P8 | Releasepad: kandidaat **0.10.2** volledig lokaal testen → push naar `main` → automatische TEST-deploy → gebruiker test beide rollen → aparte versiecommit naar **1.0.0** → handmatige PROD-goedkeuring. PROD draait Classic; Nieuw blijft uitsluitend LOCAL/TEST. | Exact bewezen code, gescheiden acceptatie en livegang. | vast |
| P6 | Automatische reminders gaan **niet** aan tot: server-side planner + per-medewerker fail-closed opt-in + dummy-only bewezen. Nu correct label "Voorbereiding · niet automatisch". | Releasekritieke productlogica. | vast |
| P7 | `allow_demo_migrations` blijft op PROD `false`. Wachtwoorden/tokens/SSH-sleutels/`config.local.php` komen niet in Git. | — | vast |
| P9 | De statische pagina's onder `/pilot/` worden uitsluitend op TEST gepubliceerd. De PROD-release moet de volledige map `pilot/` aantoonbaar uitsluiten; PROD draait alleen de echte app in Classic-weergave. | De pilots blijven beschikbaar om het herontwerp door te ontwikkelen, maar worden geen onderdeel van de eerste livegang. | vast |

## 3. Dashboard & maandkiezer

| # | Keuze | Reden | Status |
|---|---|---|---|
| D1 | Bij **elke login én elke klik op Dashboard/Home** springt de maandkiezer naar de actuele kalendermaand (Europe/Amsterdam), voor **beide rollen** (`resetHomeDashboardState`). "Dashboard" betekent zo altijd "overzicht van deze maand". Een handmatig gekozen maand blijft alleen actief zolang je op **andere** schermen navigeert (Facturen, Goedkeuringen, Mijn uren); zodra je Dashboard opent, staat de app weer op nu. Regressie: `DASH-H-018`, `DASH-H-021`, `MOB-H-022`. | Commit `c47b2f3` / `e5520b9`; Dashboard-reset toegevoegd 1.0.17 op verzoek gebruiker ("bij klikken dashboard moet je toch altijd weer naar de huidige maand"). `currentRole` wordt niet gepersisteerd — altijd uit de auth-sessie afgeleid. | vast |
| D2 | Het beheerderdashboard toont **"Werkvoorraad laden…"** tot de eerste `refreshAdminWorkflowReadApi` binnen is (vlag `readApiRuntime.adminWorkflowHydrated`), i.p.v. de lokale seed-teller die daarna omlaag corrigeert. | Commit `c085797`. Spiegelt de employee-hydratatie-gate. | vast |
| D3 | De teller die op TEST "springt" (bv. 13→14) is meestal **echte data die verandert** op de gedeelde omgeving (seeds, testruns, achtergrond-verzendingen), niet een render-bug. De toast "… e-mails verzonden" bewijst een echte mutatie. In PROD (één beheerder, mail uit, geen seeding) staat de teller stil. | — | vast |
| D4 | **Opgepakt 10 sep 2026, op expliciet verzoek gebruiker.** De eerste-frame-flits kwam van `applyAuthUiMode()` (`assets/app.js`): zodra `mode` van `"checking"` naar `"auth"` omslaat, werd `#login-screen` altijd meteen ontgrendeld — óók voor een al-ingelogde gebruiker wiens sessie een paar milliseconden later alsnog blijkt te bestaan, wat een kort inlogscherm-flitsje gaf vóór `login()` alsnog het dashboard toonde. Fix: `loginScreen.hidden` alleen nog zetten wanneer `mode !== "auth"` — voor `"demo"`/`"unavailable"` is er sowieso geen sessie om op te wachten, dus daar mag het meteen; voor `"auth"` blijft het scherm verborgen (zoals tijdens `"checking"`) totdat `login()` (bevestigde sessie, toont `#app-shell`) of `logoutLocal()` (geen sessie, toont `#login-screen`) — beide draaien synchroon in dezelfde `initializeAuthSession()`-callback als de auth-uitkomst — expliciet bepalen wat zichtbaar wordt. Bestaande regressie `AUTH-N-009` dekt dit scenario al (een vertraagde sessiecontrole na F5 waarbij beide shells verborgen moeten blijven) en bleef groen in isolatie; geen nieuwe test nodig. | Cosmetisch (nooit foute data), maar wel een zichtbare flikkering die de gebruiker liever weg had — kleine, geïsoleerde wijziging in geïsoleerde auth-bootstrap-code. | vast |
| D5 | Een medewerker ziet de actuele maand en historische maanden vanaf de eigen opgeslagen startmaand, maar **geen enkele toekomstige maand**. Voorbeeld: in oktober met startdatum 1 september zijn september en oktober beschikbaar; augustus en november niet. Als Beheer een eerdere persoonlijke startdatum opslaat, wordt die oudere historie direct beschikbaar. | Geen invoer vóór indiensttreding en geen uren vooruitboeken; Beheer houdt de expliciete correctiemogelijkheid. | vast |
| D6 | Als Beheer een startdatum later zet en daarmee bestaande uren, klanturenstaten, facturen of open acties verbergt, volgt eerst een impactsamenvatting en expliciete bevestiging. Bevestigen wijzigt alleen de zichtgrens; procesdata wordt nooit verwijderd en verschijnt exact terug als de startdatum weer wordt vervroegd. De API weigert medewerkerreads en -writes buiten de startmaand/huidige-maandgrens met `403`. | Een beheercorrectie mag echte historie niet stil vernietigen of via een directe API-call omzeilbaar maken. | vast |

## 4. Modals

| # | Keuze | Reden | Status |
|---|---|---|---|
| M1 | `closeModal(runCloseAction = false)` — de sluit-callback (`modalCloseAction`) draait **alleen bij expliciet annuleren/sluiten**, niet bij een geslaagde opslag. `revealExistingStaffAccount` herstelt `focusExistingAccount` als sluit-actie ná `reopenForm()`. | Commit `a13d26b`. Verhelpt `ADM-WR-N-004`: succesvol opslaan lichtte per ongeluk het oude conflicterende account uit. `closeAction` wordt in de hele app maar op één plek gezet, dus geen neveneffect elders. | vast |
| M2 | **`send_to_broker` dedupt de brokerlevering.** Bij een mislukte TEST-aflevering valt de brokerregel na de 1e poging terug op `queued` en de 502 laat de klanturenstaat op `approved`. Elke volgende `Controle afronden` maakte een nieuwe `email_deliveries`-regel → dubbele klanturenstaatmails. Nu: vóór insert zoeken naar een bestaande brokerregel voor deze factuur met `attachment_policy = "customer_timesheet"` (onderscheidt de klanturenstaatroute van de maandelijkse factuur-brokermail); `sent` → hergebruik zonder verzenden, `processing` → 409, `queued` → hergebruik + tekst bijwerken, niets → nieuwe insert. `server/api/customer-timesheets.php`. Happy path (`CTS-API-H-009`) ongewijzigd. | Gevonden in de exploratieronde 3 sep. | vast |

## 5. TEST-dataset ("open taken run")

| # | Keuze | Reden | Status |
|---|---|---|---|
| T1 | Eén bewaakt seed-script: **`server/scripts/seed-test-working-month.php`** (hernoemd van `seed-test-september-acceptance.php`), confirm-token `SEED_TEST_WORKING_MONTH`. | Eén bron voor de terugkerende run. | vast |
| T2 | **Kalender-relatief**: richt zich op de kalendermaand van nu, of `--month=JJJJ-MM`. Niet vastpinnen op één maand — dat brak steeds bij maandovergang. | Login opent altijd de actuele maand (D1). | vast |
| T3 | De 4 actieve TEST-medewerkers dekken samen elke open Backoffice-taak: Marc `draft` (open uren) · Brian `submitted` + klanturenstaat `received` (open goedkeuring + klanturenstaat controleren) · Stasjo `approved` + klanturenstaat `approved` + géén factuurrij (brokerroute controleren + open factuur via auto-aanmaak) · Shawn `approved` + rechtstreeks gemaild + factuurrij (verzending controleren). | Volledige taakketen in één run, inclusief open factuur + open urenstaat. | vast |
| T4 | Guards: alleen exact gedeeld TEST-contract, weigert een maand met bestaande uren/facturen, **nul e-maildeliveries**. | Nooit stil overschrijven; TEST-mailbox niet vervuilen. | vast |
| T5 | Werkwijze: eerst TEST-baseline herstellen, dan seeden, dan de open-taken run lopen. Niet tegelijk andere testruns tegen dezelfde maand draaien (anders churn zoals D3). | Reproduceerbaar. | vast |
| T6 | Invoice-PDF: nooit de kale server-fallback-PDF naar een echte mailbox laten gaan; jsPDF is verplicht. Guard in `scripts/smoke-test.mjs`. | Bestaande afspraak. | vast |

## 6. Herontwerp

| # | Keuze | Reden | Status |
|---|---|---|---|
| R1 | Het hybride herontwerp wordt incrementeel in de echte app gebouwd, maar de schakelaar **Klassiek/Nieuw** is alleen zichtbaar op LOCAL en TEST. PROD forceert Classic, ook wanneer een browser eerder `Nieuw` bewaarde. | De stabiele app kan live terwijl het herontwerp veilig naast dezelfde API wordt getest. | vast |
| R2 | Bronmateriaal blijft bewaard op branch **`design/hybrid-redesign`** en in de mockupmappen. Op `main` staat inmiddels het gescopete fundament plus het echte nieuwe medewerkerdashboard; beheer volgt later. | Implementatie met behoud van ontwerpbron en klassieke fallback. | vast |
| R3 | Ook bewaard: bewerkbare Artifact-canvas op het Claude-account (`/artifacts`), en JPG-mockups in `design-mockups/redesign-options-2026-09/` + `cohesive-redesign-1111-4444/` + `redesign-packages-2026-09/`. | — | feit |
| R4 | Hybride-richting (voor later): warm crème vlak, Roboto Slab serif-koppen, Path-groen (`#3abd9d`/`#169276`, géén teal), navy sidebar. Overnemen: "Actuele maand bij inloggen"-pill, groen/oranje/grijs-legenda, KPI-rij (aantal + %), zoeken + paginering, Documentarchief-modal met stepper bovenaan + Factuur/Urenstaat-kaarten naast elkaar + omkaderde "Externe bevestiging terugdraaien". Niet overnemen: verzonnen modules (Declaraties, Periodieke verloning, Werkgevers). | Synthese van 3 mockups. | vast |
| R5 | Ruwe schatting implementatie: visuele refresh ~3–5 werkdagen; volledige hybride ~8–15 werkdagen; grootste kost = ~380 E2E-cases groen houden bij DOM-verschuivingen. | — | referentie |

## 7. Mobiel & documentafhandeling

| # | Keuze | Reden | Status |
|---|---|---|---|
| MO1 | **Bekijken en downloaden van documenten lopen altijd via `fetch` → blob** (`openDocumentInTab` / `downloadDocument` / `deliverBlobDownload`). De `Content-Disposition` van de server (soms `inline`, soms `attachment`) bepaalt niet meer of Chrome toont of ophaalt. | "Soms wel soms niet" na downloaden op mobiel. Commit `ef6e684`. | vast |
| MO2 | **Bekijken** hergebruikt één vast tabblad `target="path-document"` (nooit `_blank`, nooit navigatie in het app-tabblad). **Downloaden** navigeert nooit. Zo neemt de PDF-viewer de web-app/PWA-sessie niet meer over. `downloadInvoicePdf` gebruikt `deliverBlobDownload` i.p.v. `doc.save()`. | jsPDF `doc.save()` viel op Android/PWA terug op navigatie naar de blob-URL. | vast |
| MO3 | `.modal-close` is **sticky** (was absolute) → het kruisje blijft in beeld ook als een lang formulier of toetsenbord-focus de modal omhoog scrolt. Donkere modus kreeg een expliciete achtergrond/kleur (was een bijna onzichtbaar wit blok). **Lichte modus ongewijzigd.** | Gebruiker kon "alleen F5" om eruit te komen. | vast |
| MO4 | Het mobiele **Hulp & contact**-paneel is boven én onder verankerd met `dvh` → de koptekst met het kruisje blijft zichtbaar nadat een FAQ-antwoord het paneel laat groeien. | Kruisje schoof achter de adresbalk. `MOB-H-019`. | vast |
| MO5 | ~~Klanturenstaatstatus is expliciet maar **niet-blokkerend**: ontbrekend/oranje; medewerker meldt rechtstreeks gemaild/oranje; Backoffice bevestigt extern of keurt upload goed/groen. Factuur mag de deur uit zodra de uren goedgekeurd zijn; klanturenstaat blijft apart openstaan.~~ **Herzien 8 sep 2026 → MO5b.** | Geen groen vinkje zonder controle. | vervangen |
| MO5b | **Klanturenstaat is verplicht en blokkerend.** Zolang de klanturenstaat van een maand niet door Backoffice is gecontroleerd: (1) kan Backoffice **"Controle afronden" / de factuurverzending niet afronden**, en (2) tonen de medewerker- én maandstatus **"Wacht op klanturenstaat"** i.p.v. "Goedgekeurd/afgerond". De stap is **oranje** in de Backoffice-werkvoorraad én in de story-/pijplijnweergave — **klassiek én new skin**. Er zijn precies twee groene paden: een ontvangen PDF/JPG/PNG wordt door Backoffice `approved`, of de medewerker kiest "Al rechtstreeks gemaild" en Backoffice voert daarna met verplichte reden **Extern bevestigen** uit. Alleen ontvangen of alleen rechtstreeks-gemaild registreren blijft oranje en blokkerend. | Gebruiker wil eerst controleren; een factuur mag niet worden verstuurd zonder gecontroleerde klanturenstaat. Besloten 8 sep 2026. | vast |

## 7b. Accountuitnodiging (0.9.163)

| # | Keuze | Waarom | Status |
|---|---|---|---|
| U1 | Bij een **nieuwe** medewerker/beheerder staat het uitnodigingsvinkje standaard **aan**; bij het **bewerken** van een bestaande persoon standaard **uit** (anders opnieuw uitnodigen bij elke wijziging). | Al zo, bevestigd. | vast |
| U2 | Op **TEST** gaat elke uitnodiging/reset naar de vaste `invitation_recipient` (`giovanno.maatsen@pathconsultancy.nl`), met `[TEST uitnodiging voor <adres>]` in het onderwerp. Nooit naar het ingevulde adres. Productie mailt wél het echte adres. | TEST mag geen echte onbekende mailen. | vast |
| U3 | De uitnodigingstekst is een **aanpasbaar kanaalsjabloon** `account_invitation` (Instellingen → Standaardteksten). Leeg = meegeleverde welkomsttekst. Tokens: `{naam} {app} {bedrijf} {link} {geldigheid}`. Vangnet: ontbreekt `{link}` in de eigen tekst, dan wordt de link er alsnog onder gezet. | Tekst mocht niet meer in de code vastzitten. | vast |
| U4 | Vaste afzender-handtekening onder de uitnodiging: **"Robot Path IT" / "Automatisering & accountbeheer"** + `support_email` + website + slogan. Wordt automatisch toegevoegd, net als bij de factuurmails. | Herkenbaar als automatisch bericht. | vast |
| U5 | Een **door de beheerder** gestuurde uitnodiging/reset (`purpose = invitation`) kent **geen** misbruikbegrenzing. De publieke "wachtwoord vergeten" houdt 3 per 15 min. | Beheerder is ingelogd; meerdere pogingen bij inrichten/testen moeten lukken. | vast |

## 8. Omgeving & techniek (feiten, geen keuzes)

| # | Feit |
|---|---|
| E1 | Dev-machine: native MySQL 8.0 + winget PHP 8.4, geen Docker. Test-DB `path_urenregistratie_test`. |
| E2 | VS Code DB Client kan de TransIP-DB's niet bereiken (ProxySQL). Data op TEST/PROD wijzigen via phpMyAdmin of SSH-shell / migraties. |
| E3 | PDO native prepares: een named placeholder mag **niet twee keer** in één query (SQLSTATE[HY093]). Brak 0.9.147, hotfix 0.9.148. |
| E4 | Display-koppen gebruiken al `"Roboto Slab", Georgia, serif`; accent `--mint` `#3abd9d` / `--mint-dark` `#169276`; `--radius: 18px`. Bron: `assets/styles.css`. |
| E5 | 24 feature-bestanden, 34 spec-bestanden. Kern-E2E-keten (`E2E-H-*`), factuurset (`INV-*`), admin-writes (`ADM-WR-*`), dashboard (`DASH-*`), mailqueue (`EQ-*`). |
| E6 | De installatienaam per omgeving komt uit **`manifest.php`** (leest `server/config.local.php` → `environment`): test = "Path TEST", development = "Path DEV", anders "Path Uren". **Alleen de naam** verschilt per omgeving; `theme_color` (statusbalk) is overal navy `#0D1B38`, gelijk aan de web-app en productie (0.9.164 — was oranje `#BB7623` op test). `index.html` linkt `manifest.php`. Vite hashte dit bestand naar `assets/manifest-XXXX.php` waardoor `__DIR__` verkeerd wees en de naam altijd op productie terugviel; `vite.config.js` houdt het nu onbewerkt in de webroot (`writeBundle` herstelt de link, `manifest.webmanifest` blijft als statische fallback). Bewaakt door `[TEST-E2E-26]`. Een al geïnstalleerde PWA hernoemt zichzelf niet — opnieuw toevoegen aan beginscherm. |

## 9. Herinneringen, PDF-bijlage en CI-opschaling (9 sep 2026 nacht)

| # | Keuze | Reden | Status |
|---|---|---|---|
| R1 | Medewerker-ontvangstmail (`timesheet_submission_receipt`) krijgt een PDF-bijlage, browser-gegenereerd bij submit (`buildTimesheetReceiptPdfBase64()`), server valideert/slaat op onder `private-root/timesheet-receipts/`. Ontbrekende/ongeldige bijlage blokkeert het indienen zelf nooit. | Eerste openstaande issue uit de mailflow-backlog. | vast |
| R2 | ~~Serverplanning voor de vier herinneringstypen draait als cron alleen op TEST; PROD bewust nog niet aangesloten.~~ **Achterhaald door R10 (9 sep, later die dag): PROD is alsnog aangesloten**, via een aparte `prod-cron`-environment zonder reviewer-poort. | Reviewer-poort en cron-frequentie zijn onverenigbaar zonder een aparte af spraak — R10 beschrijft die aparte afspraak. | achterhaald door R10 |
| R3 | `send-due-reminders.php` gebruikt `LEFT JOIN user_preferences` (was `INNER JOIN`) met `COALESCE(hour_reminders, 1)`/`COALESCE(approval_notifications, 1)`. Nergens in de API wordt voor een nieuwe medewerker een `user_preferences`-rij aangemaakt; een `INNER JOIN` sloot hen daardoor altijd stilzwijgend uit, ongeacht hun bedoelde standaardvoorkeur. | Gevonden tijdens het testen van REM-H-001 met een verse medewerker. | vast |
| R4 | CI-shards van 4 naar 8 op zowel `herontwerp` (`ci.yml`) als `main` (`release-pipeline.yml`), plus een expliciete `timeout --signal=TERM --kill-after=30s`-wrapper om de E2E-teststap: GitHub's eigen `cancel`/`timeout-minutes` reageerde herhaaldelijk traag op een vastgelopen browserproces en blokkeerde daarmee de repo-brede `release-pipeline`-concurrency-lock voor iedereen. | Gratis (publieke repo), geen self-hosted runners nodig; loste een avondvullend CI-vastloopprobleem op. | vast |
| R5 | Herontwerp's "Publish Live Docs" hergebruikt bestaande Playwright-blob-reports van de Validate-shards (`playwright merge-reports`) i.p.v. de volledige suite nogmaals te draaien. Op main was deze job tijdelijk met `if: false` uitgeschakeld; dat patroon is vervangen door hetzelfde hergebruik-mechanisme. | Elimineert de dubbele, langzame en vaak vastlopende volledige testrun puur voor documentatie. | vast |
| R6 | **Vaste opdracht "pollen en fixen":** elke ~10 minuten de laatste CI-run checken, bij rood de exacte falende stap/test opzoeken (echt de log lezen, niet aannemen), root cause fixen, gericht lokaal testen, committen/pushen, opnieuw pollen — ook na een sessie-onderbreking. Blijft gelden totdat main én herontwerp beide groen op TEST staan. Elke gevonden regressie krijgt een eigen testcase, niet alleen een losse code-fix. | Voorkomt dat een sessie stopt bij de eerste rode run of alleen symptomen bestrijdt. | vast |
| R7 | **REM-H-001 echte root cause (na een hele nacht symptomen bestrijden):** de Playwright-harness stuurt de webserver via `PATH_APP_DB_NAME`/`PLAYWRIGHT_DB_NAME`-omgevingsvariabelen naar een geïsoleerde testdatabase (`path_urenregistratie_test`), maar `server/scripts/cli-bootstrap.php` (waar de herinneringen-scheduler en losse CLI-tools op leunen) las alleen het statische `server/config.local.php` en keek zo naar een andere, grotendeels lege database dan de webserver die de suite bedient. `ops_database_config()` krijgt nu dezelfde env-var/dotenv-precedentie als `auth_db_from_config()` in `server/auth/session.php`. Dit was de enige test met een CLI+webserver-dubbelpad, wat verklaart waarom alleen deze case (en nooit lokaal) flaky was. | Grondig gediagnosticeerd via een tijdelijk debug-script dat db-naam/user-telling/host/poort/connection-id vergeleek tussen de webserver (via `bootstrap.php`/`health.php`) en een losse CLI-aanroep. | vast |
| R8 | **Publieke TEST-inlog-smoke faalde apart** (401 voor de beheerder) na R7's fix, om twee onafhankelijke redenen die na elkaar boven kwamen: (a) de `test`-environment-scoped GitHub-secret `PLAYWRIGHT_ADMIN_PASSWORD` was verouderd (stond nog op de waarde van vóór de sync naar `888888888888`, en `environment: test` geeft die scoped secret voorrang boven de recentere repo-brede secret) — gecorrigeerd via `gh secret set --env test`; (b) migraties 032/033 (die het gedeelde beheerwachtwoord naar `888888888888` zetten) stonden op de live TEST-database al als "applied" gemarkeerd van vóór de fix, en de migratie-runner trackt op bestands-id, niet op inhoud — latere edits aan die bestanden hadden dus geen effect meer. Toegevoegd: migratie `035_demo_admin_password_final_correction.sql` (verse id, draait wel echt). | Geen van beide had iets met REM-H-001 zelf te maken; kwamen pas aan het licht doordat deploy-test (voorheen geblokkeerd door de rode Validate-run) voor het eerst weer succesvol draaide. | vast |
| R9 | **De écht persistente bron van het TEST-beheerwachtwoord bleek `server/migrations/005_demo_auth_hashes_for_existing_seed_users.sql`**, niet 032/033/035: `server/scripts/reset-test-baseline.php` draait op élke TEST-deploy direct na `migrate.php`, doet `TRUNCATE TABLE users` en herzaait vanaf `database/seed-demo-data.sql` (zonder `password_hash`) + migratie 004/005 (die het wél zetten, maar alléén als de hash NULL/leeg is — wat na een truncate altijd zo is). Voor het exacte live TEST-contract preserveert de capture/restore-mechaniek in `server/lib/test-reset.php` bewust alléén de twee acceptatie-accounts, niet de gedeelde demo-accounts — dus 032/033/035's UPDATE op een bestaande rij werd bij de eerstvolgende deploy altijd weer overschreven door 005's (nog oude) `LocalDemoAdmin2026`-hash. 005 en `test_reset_verify_remote_demo_credentials()` in `test-reset.php` zijn nu bijgewerkt naar `888888888888`; ook `tests/remote/test-site-smoke.spec.ts` en `scripts/verify-against-localhost.mjs` (lokale tools) voor consistentie. Onafhankelijk bevestigd via een directe curl-login tegen de live TEST-site (HTTP 200). | Elke toekomstige wijziging van het gedeelde demo-wachtwoord moet in 005 (en `test-reset.php`'s verify-check), niet alleen in een losse correctiemigratie — anders overleeft hij de eerstvolgende TEST-deploy niet. | vast |
| R10 | **PROD-herinneringen alsnog geactiveerd (9 sep, op expliciet verzoek).** Nieuwe environment `prod-cron` (géén `required_reviewers`, bewust smal: alleen deze ene idempotente, niet-destructieve actie leunt erop) met eigen `TRANSIP_SSH_PRIVATE_KEY`/`TRANSIP_SSH_KNOWN_HOSTS`-secrets én `TRANSIP_SSH_HOST`/`TRANSIP_SSH_USER`-variabelen (environments delen die laatste twee niet automatisch met de bestaande `prod`-environment — miste dit eerst, cron zou stil op een lege SSH-host gefaald hebben; nu gezet). **Losstaande, belangrijkere ontdekking tijdens het verifiëren:** `configure-production-mail-pilot.php --mode=check` op de echte PROD-server meldde `"current_production_mode": "live"` — geen `pilot` of `disabled` zoals het go-live-plan (`PROD-LIVE-HANDOFF-2026-09-01.md`) beschreef. `live` is een derde stand die dat script zelf niet kan zetten (alleen `pilot`/`disabled`) en die de allowlist-beperking volledig omzeilt: `mail_recipient_is_allowed()` en `mail_real_delivery_allowed_for_environment()` geven bij `live` altijd `true`, voor elk adres. PROD-mail was dus al voor iedereen open, niet dummy-only — de oorspronkelijke voorzichtige pilot-opzet is op enig moment (buiten deze sessie om) al gepasseerd. | Voorkomt dat een volgende sessie het `--mode=pilot`-commando draait in de veronderstelling dat dit "activeren" is — bij een PROD die al op `live` staat is dat juist een **beperking** (downgrade naar alleen de opgegeven adressen). | vast |
| R11 | **Twee sticky-topbar-scroll-margin-bugs gevonden en gefixt** (Instellingen-sectienavigatie + dashboard "Open werkvoorraad"): `scroll-margin-top: 18px` was veel te weinig tegen de 88px-hoge sticky `.topbar`, waardoor een sprong de doelkop verstopt achter de balk liet landen. Nu 104px (desktop) / 300px (mobiel, topbar daar ~292px). Twee andere kandidaten (`employee-open-overview`, `dashboard-team-title`) leken op hetzelfde probleem maar bleken bij screenshotcontrole geen bug: de sticky topbar is op die scrolldiepte zelf al voorbij beeld (normaal `position:sticky`-gedrag bij een lange scroll), dus geen overlap. Regressietests `ADM-WR-H-021`/`ADM-WR-H-022`. | Bewijst het nut van eerst screenshotten vóór een "consistente" fix wordt toegepast — de twee niet-bugs waren zonder controle bijna ten onrechte ook gewijzigd. | vast |
| R12 | **PWA-installatiebanner overlapt soms een knop op een korte mobiele pagina** (`assets/pwa-install.js`): de bestaande `body.toont-installatieaanbod .main-content { padding-bottom: 190px }`-fix helpt alleen tegen content aan het écht einde van een lange pagina, niet tegen content die al binnen het eerste scherm staat op een korte pagina. Bewust niet gefixt: de balk is al goed ingeperkt (12s auto-hide, één tik weg, 30 dagen stil daarna) en een sluitende fix kost blijvend scrollruimte voor iedereen. | Overwogen en bewust laten staan na overleg — niet vergeten, gewoon geen goede kosten/baten. | open (bewust) |
| R13 | **SKIN-H-008 definitief opgelost (10 sep, herontwerp-peer) — géén productbug.** Root cause: `dashboardVoor` werd gemeten zodra er één medewerkersrij in `#dashboard-employee-rows` stond, niet zodra `readApiRuntime.adminWorkflowHydrated` echt klaar was; bij trage/late hydratatie ving de eerste meting soms de voorlopige staat en de tweede (na de navigatieronde) de echte, wat als een "wijziging" oogde. Stasjo's 24u in september bleek gewoon legitieme demo-seeddata (rechtstreeks via de server-API bevestigd, vóór enige navigatie) — geen mutatie. Dit verklaart ook waarom main se Validate-shard 8 los hiervan gewoon groen bleef: main viel toevallig nooit op het verkeerde hydratatiemoment. Eerdere `broker.trade_name`-hypothese (9 sep, hieronder oorspronkelijk vastgelegd) is met deze vondst uitgesloten als oorzaak. Fix in `tests/playwright/skin.spec.ts`: wacht op `#dashboard-next-action-label` (niet meer "laden…") vóór `dashboardVoor`/`teamTitelVoor` gemeten worden — zelfde patroon als `DASH-N-017`. 5/5 lokaal deterministisch groen bij de peer. | Eerst dieper gegraven i.p.v. de eerste plausibele hypothese (broker-race) te fixen — de werkelijke oorzaak zat in de test se meetmoment, niet in productiecode. | vast |
| R14 | **CI-shard-storing door Google's eigen Chrome-apt-repo** (`dl.google.com/linux/chrome-stable/deb`), niet onze code: alle 8 Validate-shards faalden identiek op "Hash Sum mismatch" tijdens `Install Playwright browser`, vóór er één test draaide. Externe, tijdelijke inconsistentie in Google's package-index (Release-file-timestamp lag ná de Packages-index-timestamp). Opgelost met `gh run rerun --failed`, geen codewijziging nodig. | Herkenningspatroon voor een volgende sessie: alle shards tegelijk falen op exact dezelfde installatiestap, nooit op een test zelf → eerst rerunnen vóór root-cause-analyse in eigen code. | vast |
| R15 | **Stasjo (echt medewerkersaccount) mag op TEST zijn eigen wachtwoordreset écht ontvangen, en alléén dat kanaal.** Nieuwe functie `mail_test_extra_password_reset_recipients()` (`server/mail/config.php`) + `acceptance_test.extra_password_reset_recipients` in `configure-test-mail-sandbox.php`: een met naam genoemd adres omzeilt de TEST-sinkomleiding uitsluitend voor `channel === password_reset`; elk ander mailtype voor hem blijft omgeleid naar de sink (Gio/Kenrich), net als voorheen. Bewust *niet* via de bestaande brede allowlist-tool opgelost (die maakt van hem een algemene testontvanger zoals Kenrich, breder dan gevraagd). **Bekende beperking:** zijn echte e-mailadres staat alleen op zijn `users`-rij, niet in de seed — `reset-test-baseline.php`'s TRUNCATE+reseed (elke TEST-deploy) zet 'm terug naar het placeholder-adres `stasjo@example.invalid` (dat adres is bewust *niet* aangepast: het is de standaard `PLAYWRIGHT_EMPLOYEE_EMAIL` voor de hele Playwright-testsuite). Het e-mailadres moet dus na elke TEST-deploy opnieuw gekoppeld worden (Medewerkers → Stasjo → Gegevens aanpassen), tenzij `test_reset_capture_baseline_credentials()` in `server/lib/test-reset.php` ooit wordt uitgebreid om ook e-mailadressen te bewaren (bewust nog niet gedaan: raakt kerninfrastructuur die elke CI-run gebruikt). | Voorkomt dat een volgende sessie zich afvraagt waarom Stasjo's echte adres "spontaan" weer verdween na een TEST-deploy. | vast (met bekende beperking) |
| R16 | **Pilot-merge-wachtrij mag niet wachten op de handmatige PROD-poort.** `pilot-merge-queue.yml` wacht terecht zolang main nog Validate/TEST/Live Docs/deploywerk doet, maar een Release Pipeline die `Deploy Test to TransIP` al succesvol afgerond heeft en alleen nog `Promote Prod*`-jobs open heeft, is geen blokkerende main-release meer. Die PROD-poort is bewust handmatig en mag nieuwe groene herontwerp-promoties naar main/TEST niet drie uur ophouden. De wachtrij inspecteert daarom jobniveau via `listJobsForWorkflowRun`; `deployment-contract-check.mjs` bewaakt dat alleen een groene TEST-deploy met uitsluitend open `Promote Prod`-jobs genegeerd wordt. **Zelfde regel geldt voor een sessie die zelf een pipeline-run volgt** (niet alleen voor de geautomatiseerde wachtrij): zodra alles vóór de PROD-poort groen is en alleen die poort nog `waiting` staat, is dat een afgeronde toestand, niet "nog bezig" — niet blijven pollen, kort melden en doorgaan met het volgende. Een later overschreven wachtende PROD-poort (door een nieuwere push, via de concurrency-lock) kan bovendien als `failure` tonen zonder dat er iets misging — controleer bij zo'n conclusie eerst of er een nieuwere run bestaat voor dezelfde branch voordat je een echt probleem vermoedt. Zie ook §8b in herontwerp's `HANDOFF-CODEX-FASE-D.md`, waar dit onafhankelijk hetzelfde bevestigt. | Gevonden tijdens monitoring van run `34392011626` en wachtrijrun `34394181535` (TEST was groen, main bleef `waiting` door PROD-review); herbevestigd 10 sep bij run `34415621126` waarvan de Promote Prod-jobs op `failure` stonden puur omdat een latere push (`34420061048`) de wachtende poort overschreef. | vast |
| R17 | **Validate-shard 8 (mobile-safari) is structureel de meest instabiele shard**, breder dan alleen `SKIN-H-008` (R13) — en de oorzaak is nu **hard bevestigd, geen vermoeden meer**: `npx playwright test --list --shard=N/8` laat zien dat Playwright's ingebouwde sharding simpelweg de gedeclareerde `projects`-volgorde in gelijke stukken knipt, zónder te wegen naar snelheid/betrouwbaarheid per engine. Resultaat: shard 1 (en de meeste ervoor) = 100% `desktop-chromium`, shard 7 = 67 `mobile-chrome` + 1 losse `mobile-safari`, **shard 8 = 68/68 (100%) `mobile-safari`**. Shard 8 draait dus als enige zónder enige verdunning door de snellere/stabielere chromium-projects, wat zowel de langere looptijd (WebKit-emulatie is inherent trager) als de flakiness (WebKit-eigenaardigheden rond `waitForEvent("download")`, timing) volledig concentreert in één shard i.p.v. te spreiden. Twee opeenvolgende runs op dezelfde commit-reeks (`34414073007` en `34415621126`) faalden allebei op exact shard 8, maar elke keer op **andere, onderling ongerelateerde tests** (1e run: `E2E-H-024` hard, `E2E-H-003`+`SKIN-H-008` flaky; 2e run: `business-workflows-retention`/`business-workflows-lock`/`SKIN-H-020`-CSV-download hard/flaky, `SKIN-H-008` weer flaky) — nooit dezelfde assertie twee keer, en geen van de gefaalde tests raakte code die in die commits gewijzigd was. Opgelost met `gh run rerun --failed` (2x), geen codewijziging. **Mogelijke structurele verbetering (niet doorgevoerd, bewust):** Playwright's projects-declaratievolgorde omdraaien of interleaven zodat elke shard een mix van engines krijgt zou de flakiness spreiden i.p.v. concentreren — niet zelf doorgevoerd omdat dit `playwright.config.ts` + de sharding-aannames in `release-pipeline.yml`/`ci.yml`/`live-docs.yml` raakt, een bredere/risicovollere wijziging dan gerechtvaardigd zonder overleg. | Voorkomt dat een volgende sessie bij een falende shard-8-run meteen naar codewijzigingen zoekt: eerst checken of de falende tests wisselen tussen reruns (= omgeving) of steeds dezelfde zijn (= mogelijk echt). | vast (met niet-doorgevoerd verbetervoorstel) |
| R18 | **`E2E-H-024` (wachtwoordreset via GUI) hoort niet zomaar bij R17's shard-8-ruis** — was een hárde 2/2-fail (niet "flaky"), en er is een concreet, aanwijsbaar mechanisme gevonden (niet bevestigd als dé oorzaak, wel plausibel, twee lagen diep): (1) `showPasswordResetForm()` wordt in `assets/app.js:13837-13839` **twee keer** aangeroepen voor dezelfde paginalading — één keer synchroon vanuit `consumePasswordResetFragment()`, en nogmaals in de `.finally()` van de asynchrone `initializeAuthSession()` (een netwerkcall naar de auth-sessiecheck). (2) Belangrijker: als die asynchrone auth-check op dat moment een (mogelijk nog niet volledig verwerkte) actieve sessie aantreft, doorloopt `initializeAuthSession()` zijn "authenticated"-tak (`applyAuthUserToState` → volledige login-flow, `state.currentRole` wordt gezet) — en zodra ná die tak alsnog `showPasswordResetForm()` opnieuw draait, ziet die functie `state.currentRole` gezet staan en roept **zelf nogmaals `logoutLocal()`** aan (`assets/app.js:9697`, `if (state.currentRole || authRuntime.authenticated) logoutLocal()`), die op zijn beurt `#app-shell`/`#login-screen` omschakelt, pickers herbouwt en `window.location.hash` reset. Deze hele cyclus (login-flow zetten → weer afbreken → formulier opnieuw tonen/focussen) kan precies samenvallen met de test se fill+click, wat verklaart waarom de klik de handler niet lijkt te bereiken (leeg + verborgen feedback-element, zie faallog run `34414073007`). De test doet zelf wél een expliciete `POST /server/auth/logout.php` vóór het navigeren — dit spoor veronderstelt dus een resterende timing-marge tussen die logout en de daaropvolgende `initializeAuthSession()`-call, niet bevestigd met een trace. Niet zelf gefixt: geen zekerheid zonder een gevangen trace/video van déze specifieke test, en de voor de hand liggende fix (de dubbele `showPasswordResetForm()`-aanroep weghalen, of `logoutLocal()` daarbinnen indammen) raakt een stuk auth-opstartcode dat voor elke login/reset-pagina geldt — te risicovol om blind aan te passen. | Een volgende sessie met een gevangen `E2E-H-024`-fail (trace.zip) kan meteen op dit tweelaags-spoor controleren i.p.v. opnieuw vanaf nul te zoeken. | open |
| R19 | **Testfeedback (Stasjo, medewerker, screenshot van TEST-mobiel):** vroeg om "een knop om van Classic en design terug te kunnen schakelen en andersom" — die knop (`#quick-skin-toggle`, de groene "Nieuw"-pil naast de bel) bestond al en werkte al beide kanten op (`toggleQuickSkin()`), maar toonde alleen de huidige stand zonder enige tik-hint; op mobiel is er geen hover-tooltip om de `title`/`aria-label` te ontdekken, dus las de pil als statuslabel i.p.v. knop — hetzelfde onderliggende probleem als de eerdere licht/donker-indicatorfeedback (zie de `styles-new.css`-fix in het herstelde-hero-blok-verhaal), nu voor de andere schakelaar. Pictogram gewijzigd van het neutrale rastericoon (◫) naar een wisselicoon (⇄) in `assets/app.js` (`syncAppearanceSwitches()`) én de statische `index.html`-fallback; gedrag zelf ongewijzigd. Geen test haakte op het oude pictogram (alleen op `#id` en zichtbaarheid), dus geen testwijziging nodig; `SKIN-H-001`/`SKIN-H-021` gericht herhaald ter controle. | Voorkomt dat een volgende sessie een tweede, overbodige schakelknop bouwt naast een al werkende — het probleem was zichtbaarheid, niet functionaliteit. | vast |
| R20 | **Testfeedback (gebruiker): "uren boeken en refresh F5 gaat fout, ook bij oud design."** Bevestigd: ingevoerde uren waren na F5 weg. Root cause: `scheduleDraftTimesheetWrite()` (`assets/app.js`) vertraagt elke conceptopslag bewust 700ms (debounce, om niet bij elke toets een verzoek te sturen), maar er was géén `beforeunload`-bescherming — verversen/sluiten binnen dat venster annuleerde de aflopende timer of de al lopende fetch zonder enige waarschuwing. Trof beide skins gelijk, want `writeRuntime`/`scheduleDraftTimesheetWrite()` is gedeelde code, niet skin-specifiek (bevestigt de eigen melding "ook bij oud design"). Fix: een `beforeunload`-listener die de native browserwaarschuwing toont (niet met eigen tekst overschrijfbaar) zolang `writeRuntime.draftTimer` of `writeRuntime.draftInFlight` nog actief is. Nieuwe regressie `[E2E-H-028]` in `business-workflows-idempotency.spec.ts` bewijst het mechanisme rechtstreeks (een `beforeunload`-event dispatchen en `defaultPrevented` controleren, vóór en ná een bevestigde opslag) in zowel het klassieke rooster (`input`-event, bij elke toets) als het Nieuw-bento-uurveld (dat op `change` opslaat, bij blur/Enter) — een `page.reload()` zelf gebruiken is niet betrouwbaar omdat Playwright een `beforeunload`-dialoog standaard wegklikt. Nog niet naar herontwerp gemerged op het moment van schrijven; volgt via de normale main→herontwerp-merge, geen apart risico want de wijziging zit in gedeelde code. | Voorkomt dataverlies bij een veelvoorkomende, volkomen normale gebruikershandeling (typen + meteen verversen); een 700ms-venster is kort maar makkelijk te raken. | vast |
| R21 | **Testfeedback (gebruiker, screenshots licht en donker): "graag groene balk laten zien zodat je weet dat je kan schuiven"** — de week-knoppenrij in Mijn uren (`#hours-week-filter`, ongescoped/gedeeld tussen Klassiek en Nieuw) is op mobiel breder dan het scherm en leunde alleen op de native `::-webkit-scrollbar`-styling (mint/groen) om dat te laten zien. Veel mobiele browsers verbergen een overlay-scrollbar echter tot je er actief op raakt, dus een stilstaand scherm toonde niets — precies wat de twee screenshots lieten zien (in het ene moment wel een balkje, in het andere niet, ondanks gelijk thema). Fix: een eigen, altijd-zichtbare balk die geen overlay-gedrag heeft, met breedte/positie berekend uit echte `scrollWidth`/`clientWidth`/`scrollLeft`. Gebruikt dezelfde themakleuren (`--line`/`--mint`) als de rest van de app, dus werkt identiek in licht en donker. **10 sep, gegeneraliseerd (zie R24):** het mechanisme zat eerst hardgecodeerd aan `#hours-week-filter` vast (`#hours-week-scroll-track`/`-thumb`, `updateHoursWeekScrollThumb()`); nu generiek voor elke `.segmented-control` via `refreshSegmentedControlScrollIndicators()`. Regressie: `[MOB-H-002]` (bijgewerkt naar de generieke selector). | Een native overlay-scrollbar is geen betrouwbare "je kunt hier schuiven"-hint op mobiel — een eigen, permanent zichtbaar element wel. | vast |
| R22 | **Testfeedback (gebruiker, screenshots): "0 8 9 bij oud en new zetten"** — de 0/8/9-snelkeuzeknoppen bestonden al in Nieuw se bento-uurkaartjes (`.new-bento-presets`), maar `display: none` + alleen `:is(.is-active, :focus-within)` toonde ze uitsluitend bij de actief geselecteerde/gefocuste dag; bij de andere dagen (het merendeel) waren ze onvindbaar zonder eerst te tikken. Klassiek had de knoppen helemaal niet, alleen het kale getalveld. Gekozen (via vraag aan gebruiker): altijd zichtbaar per dag, in beide skins. Nieuw: de gating-regel in `styles-new.css` verwijderd, presets staan nu standaard op `display: grid`. Klassiek: nieuwe `.hours-day-presets`-knoppenrij toegevoegd in `assets/app.js` (dezelfde `renderHoursGrid()`-celopbouw) + CSS in `styles.css`, met een eigen `data-hours-set`-klikhandler die het `input`-event dispatcht (klassiek slaat op `input`, niet op `change` zoals het bento-veld). Bijkomstige correctie: `.hours-day-entry` was een `<label>` die de hele cel omwikkelde — met geneste knoppen erin kan een label een klik ongewenst nogmaals doorzetten naar het input-element; nu een `<div>` (het veld heeft al een eigen `aria-label`, geen functioneel verlies). Regressie `[SKIN-H-025]` bewijst beide skins in één scenario: minstens 2 dagen tonen allebei hun 0/8/9 zonder focus, en een klik zet de juiste waarde. | Voorkomt dat een snelkeuze bestaat maar in de praktijk onvindbaar blijft, en dat beide skins verschillende functionaliteit bieden voor exact dezelfde handeling. | vast |
| R23 | **Gevonden door de herontwerp-peer, gedeelde code (`assets/app.js:3508` `syncInvoiceStatusesFromApi()`):** `record.invoiceStatus` kan naar `"simulated"` springen (regel ~3571, `hasDeliveryProof \|\| row.status === "simulated"`) onafhankelijk van of `record.timesheetStatus` al lokaal is bijgewerkt naar `"approved"` — de twee velden worden niet atomair samen bijgewerkt, ondanks dat de rest van de functie juist zorgvuldig precedentie tussen server-/lokale-status regelt (zie de uitgebreide commentaren erboven over `hasVersionedTimesheetState`/`isFreshlyApproved`). Symptoom: een "Mailvoorbeeld"-toast wordt afgewezen op een rij die zelf nog "Ready"/klaar-voor-controle toont — intern inconsistente status tussen de twee velden. Financiële logica; bewust **niet** blind gepatcht door de peer, en ook hier niet overgenomen zonder een concrete falende testcase — dit is exact het soort reconciliatiecode waar een snelle aanname (zoals bij R18's `beforeunload`-spoor) makkelijk een ander edge case breekt dat de bestaande commentaren al bewust afdekken. | Cross-branch gedeeld gevonden, dus hier ook vastgelegd i.p.v. alleen in herontwerp's eigen handoff — een volgende sessie op main kan het anders missen. | open |
| R24 | **Op verzoek ("kijk voor verbeteringen") gegeneraliseerd, 10 sep 2026:** R21's schuifbalk-indicator zat hardgecodeerd aan `#hours-week-filter` vast. Bij het nalopen bleek `.segmented-control` een gedeeld patroon (6 plekken: Mijn uren, Goedkeuringen, 2x Facturen, Mededelingen-archief, Medewerkers), en **alleen** `.hours-week-filter` had zijn eigen `overflow-x: auto` — de andere 5 hadden dat niet eens, dus liepen bij genoeg knoppen gewoon uit hun bak i.p.v. te schuiven. Beide dingen nu op de basisklasse `.segmented-control` gezet in `assets/styles.css` (overflow-x + de altijd-zichtbare balk via `.segmented-control-scroll-track`/`-thumb`), en de JS omgebouwd naar één generieke `refreshSegmentedControlScrollIndicators()` (`assets/app.js`) die de balk als sibling aanmaakt/verwijdert per `.segmented-control`, aangeroepen aan het eind van `renderAll()` plus direct na elke dynamische filter-herbouw (`renderHoursWeekFilter`, `renderApprovals`) en bij `resize`. Nieuwe regressie `[MOB-H-025]` bewijst het generieke mechanisme zelf los van app-data (synthetische overlopende/passende rij, en dat de balk weer verdwijnt zodra een rij krimpt) — `[MOB-H-002]` bijgewerkt naar de nieuwe generieke selector. | Eén hardgecodeerde fix per gevonden probleem stapelt zich op; een generieke, herbruikbare oplossing dekt ook toekomstige `.segmented-control`-toevoegingen automatisch mee. | vast |
| R25 | **Mobiele testdekking uitgebreid, cross-branch binnengehaald.** Gebruikersverzoek ("loop door alle cases, kijk naar het verschil mobiel/desktop") viel samen met een al lopend onderzoek van de herontwerp-peer naar exact dit gat (playwright.config.ts's mobile-chrome/mobile-safari-projects hadden een `testMatch`-allowlist van maar 3 patronen op een totaal van 8 "UI Desktop"-bestanden). Bewust **niet gedupliceerd** — peer maakte het af (commit `46e400c`, herontwerp), main heeft die ene commit via `git cherry-pick -x` binnengehaald (v1.0.53): `dashboard.spec.ts` + `help-widget.spec.ts` (88 cases) draaien nu ook op mobile-chrome/mobile-safari. Vier echte viewport-bugs gevonden en gefixt onderweg (zie de cherry-gepickte commitboodschap): desktop-only `#switch-role`-knop i.p.v. `#mobile-switch-role`, een dichtgeklapte instellingenkaart die een knop op `display:none` hield, een open hulp-paneel dat de uitlogknop blokkeerde, en de PWA-installbanner die nooit onderdrukt werd in deze twee bestanden. Op main opnieuw lokaal geverifieerd (niet blind overgenomen): 38/38 (dashboard) + 6/6 (help-widget) op zowel mobile-chrome als mobile-safari. Nog 5 "UI Desktop"-bestanden ongedekt op mobiel (accessibility/auth/invoices/pilot-page/timesheet-review-ui) — geen van beide kanten opgepakt, bewust nog open. | Voorkomt dubbele/tegenstrijdige config-wijzigingen op hetzelfde bestand vanaf twee kanten tegelijk; expliciete taakverdeling via cross-sessie-overleg i.p.v. allebei blind doorwerken. | vast (met 5 bestanden nog open) |
| R26 | **Resterende 5 "UI Desktop"-bestanden uit R25 nu ook op mobiel** (`accessibility.spec.ts`, `auth.spec.ts`, `invoices.spec.ts`, `pilot-page.spec.ts`, `timesheet-review-ui.spec.ts` toegevoegd aan `playwright.config.ts`'s mobile-chrome/mobile-safari `testMatch`), na expliciet overleg met de herontwerp-peer om overlap te voorkomen (peer bevestigde geen van deze bestanden aangeraakt te hebben). Eén echte viewport-bug gevonden en gefixt in `tests/playwright/pilot-page.spec.ts` (`PILOT-H-006`): de knop "Indienen ter controle" staat direct onder een regel waarvan de snelkeuzestrook (`.quick`, CSS `:focus-within`) inklapt zodra focus daarvandaan verdwijnt. Playwright's synthetische klik bepaalt het klikpunt vóór de dispatch; de klik op "Indienen ter controle" blurt zelf de eerder gefocuste stapperknop, waardoor de kaart *tijdens* diezelfde klikreeks 60px omhoog schuift en de klik naast de knop landt (bevestigd via `document.elementsFromPoint`/`pointerdown`-tracing: bounding box vóór de klik x60px lager dan erna). Dit is geen testartefact alleen — een snelle, aaneengesloten tik van een echte mobiele gebruiker (stapper-tik direct gevolgd door tik op de indienknop) kent hetzelfde risico, omdat een browser `touchstart`/blur-reflow vóór `touchend` kan laten plaatsvinden. Niet in de (bevroren, 1:1-mockup) pilot-CSS gefixt gezien het beperkte, moeilijk zonder ontwerpwijziging te elimineren risico op deze statische pilot-pagina; wel in de test verholpen door de focus expliciet te blurren vóór de volgende klik, zodat de layout eerst settelt (zelfde patroon als een echte gebruiker die de kaart ziet stabiliseren vóór de volgende tik). Overige 5 bestanden 65/67 (mobile-chrome) groen; twee bekende, niet-mobielspecifieke issues apart gehouden: `TS-REV-UI-H-012` (companies[0] undefined in `companySettings.ts`-helper) en de reeds bestaande CI-brede mobile-safari-resourceflake (zie R27). | Playwright's klikstabiliteit-aanname (één vast klikpunt voor de hele mousedown→click-reeks) kan een layoutshift veroorzaakt door de klik zelf niet detecteren; vastgelegd zodat een volgende sessie dit patroon herkent i.p.v. het als losse flake te bestempelen. | vast |
| R27 | **`DASH-H-008` (GUI-closeout, 12 taken) faalt op `Promote Test (7)`, mobile-safari, na retry** (run `34469272704`, v1.0.53) — `openDemoAdmin`/`openDemoEmployee` lopen tegen dashboard-navigatie/login-timing vast (`#view-dashboard` krijgt `is-active` niet op tijd; retry faalt zelfs op `#login-employee-choices` dat niet zichtbaar wordt). Zelfde patroon als de al gedocumenteerde mobile-safari-CI-resourceflake (R13/R17/R18): een grote, veel-stappen-test op een zwaarbelaste CI-runner. Nieuw omdat `dashboard.spec.ts` pas sinds R25 (v1.0.53) op mobiel meedraait — dus dit is de eerste keer dat deze specifieke test onder mobile-safari-CI-belasting is gezien, niet per se een nieuwe bugklasse. Niet blind herprobeerd/gefixt: geen trace bekeken, geen patroon van 2+ onafhankelijke runs zoals bij R13 nodig was om een echte oorzaak te onderscheiden van ruis. | Vastgelegd i.p.v. genegeerd, zodat een volgende falende run op dezelfde test meteen als mogelijk patroon (i.p.v. op zichzelf staand) herkend wordt. | open |
| R28 | **Echte, breed-impacterende a11y-bug gevonden en gefixt via mobile-safari-dekking (R26): focus keerde na het sluiten van een dialoog nooit terug naar de knop die hem opende, op elk WebKit/Safari-apparaat (dus ook echte iPhones/iPads).** `showModal()` vulde `modalOpenerElement` met `document.activeElement` op het moment van openen -- maar WebKit/Safari geeft een `<button>` bij een muis-/touchklik géén focus (in tegenstelling tot Chromium/Firefox, een bekend cross-browser verschil), dus `document.activeElement` was daar vrijwel altijd nog gewoon `<body>`. `closeModal()`'s focus-terugzetlogica (bewust aanwezig, met duidelijke toelichting) deed daardoor op WebKit stilzwijgend niets: de toetsenbord-/schermlezerpositie viel na elke dialoogsluiting op de kale pagina, voor elke van de tientallen `showModal()`-aanroepen door de hele app. Gefixt met een aparte, capture-phase `pointerdown`-listener (`lastPointerActivatedElement`) die bijhoudt welk klikbaar element het laatst werd aangeraakt, als fallback wanneer `document.activeElement` niets bruikbaars is (`assets/app.js`). Bevestigd via `A11Y-H-004`: faalde 100% op mobile-safari, nooit op desktop-chromium (Chromium focust knoppen wel bij klik) -- exact dezelfde onderliggende WebKit-eigenaardigheid als het losse Playwright-testtimingprobleem in R26/PILOT-H-006 (daar was de klik zelf het probleem, hier de afwezige browserfocus erna), niet dezelfde oorzaak maar wel hetzelfde "WebKit wijkt af van Chromium bij klik-gerelateerde focus" thema. | Dit bleef tot nu toe onopgemerkt omdat vóór R26 geen enkele a11y-test op een écht WebKit-apparaat draaide -- desktop-chromium-only dekking mist dit soort browserverschillen structureel. Reden om R26/R25's mobiele dekkingsuitbreiding niet als "alleen meer van hetzelfde" te zien. | vast |
| R29 | **`A11Y-H-006`/`SKIN-H-015` (via `openProfielmenu()` → "Voorkeuren" klikken) blijken ook op een schone CI-runner te falen**, niet alleen lokaal: run `34482900830` (v1.0.57) faalde hard op `Validate (8)`, mobile-safari, zelfs na CI's eigen retry (2/2 identiek — geen ruis-die-op-retry-doorloopt). Geen datacorruptie of duidelijke productbug gevonden bij onderzoek: `toggleTopbarPopover()`/`closeTopbarPopovers()` in `assets/app.js` zijn zuiver synchrone DOM-toggles zonder animatie/transitie die het zou kunnen vertragen, en `showPreferences()` bouwt alleen synchroon HTML op (geen fetch/async werk). Screenshot op zowel CI als lokaal toont hetzelfde beeld: het profielmenu-paneel opent nooit, de knop zelf blijft intact zichtbaar. Eigen dieper lokaal onderzoek (herhaald klikken + console/pageerror-logging) liep vast op een oncontroleerbare, zelf-veroorzaakte 500-fout tijdens inloggen — een aparte, niet-gerelateerde lokale-resourcecontentiehap na de vele testruns deze sessie, geen bruikbaar resultaat. Mitigatie toegepast i.p.v. verder blind gokken: `openPaneel()`'s interne budget (`TopbarMenu.ts`) van 1s/15s naar 2,5s/20s, en de drie afhankelijke call sites (`accessibility.spec.ts` ×2, `skin.spec.ts` se `kiesVormgeving()` + het losse `SKIN-H-015`-blok, nu ook in een `toPass`-vangnet) naar 2,5s/25s — ruim boven het interne budget, zelfde precedent als SKIN-H-008's 10s→20s. **Bevestigd op run `34489217351` (v1.0.58): `Validate (8)` (met A11Y-H-006/SKIN-H-015) nu groen** — maar `Promote Test (8)` op diezelfde run faalde er alsnog op, wat de tijdsbudget-theorie ontkrachtte. **Definitieve oorzaak gevonden via een tijdelijke `console.trace()` in `closeTopbarPopovers()`**: `window.addEventListener("scroll", ...)` (`assets/app.js`) sluit elk open topbar-paneel bij een scroll-event, met een bestaande `LAYOUT_SCROLL_GRACE_MS`-vrijstelling (300ms) voor scrolls die vlak na een hertekening komen — maar Playwright's eigen "scroll element in beeld" vóór een klik op "Voorkeuren" (dat pas ná de eerdere actionability-polling-cycli gebeurt, dus regelmatig >300ms na het openen) viel BUITEN die vrijstelling en sloot zo het net-geopende profielmenu vlak vóór de klik erop landde -- een zichzelf ondermijnende race, geen datacorruptie. Gefixt: (1) `toggleTopbarPopover()` roept nu zelf `markLayoutRender()` bij het openen; (2) `LAYOUT_SCROLL_GRACE_MS` van 300ms naar 1500ms; (3) `A11Y-H-006` kreeg `test.setTimeout(90_000)` (twee skin-iteraties × twee keer Voorkeuren openen liep tegen de standaard 45s-testlimiet aan). Lokaal herhaald bevestigd (A11Y-H-006 2/2, SKIN-H-015 1/1, desktop-chromium 6/6 ongewijzigd). | Twee opeenvolgende schone CI-runs met hetzelfde symptoom was genoeg om het niet als "lokale flake" af te doen, maar pas een `console.trace()` op de daadwerkelijke sluitfunctie gaf de zekere oorzaak -- tijdsbudgetten oprekken (de eerste, verkeerde aanname) had het toevallig soms gemaskeerd zonder de race zelf op te lossen. | vast |
| R31 | **`Validate (10)` op diezelfde run `34489217351` faalde breed en niet-specifiek**: 4 volledig ongerelateerde tests (`timesheet-review-ui.spec.ts` ×2, `mobile-ui.spec.ts`, `skin.spec.ts`) faalden allemaal exact op dezelfde plek — `LoginPage.login()`'s `page.waitForFunction` (12s) op `#app-shell` zichtbaar worden, na een geslaagde klik op de inlogknop. 3 van de 4 herstelden op CI's eigen retry (flaky), maar `TS-REV-UI-H-008` faalde zelfs 2/2 identiek. Dit patroon (meerdere totaal ongerelateerde tests die allemaal op exact dezelfde generieke stap vastlopen, binnen één shard/runner) is de handtekening van een overbelaste CI-runnerinstantie (zelfde categorie als R13/R17/R18/R27), niet een codewijziging: geen van de wijzigingen deze sessie raakt `LoginPage.login()` of de inlogflow. Niet blind "gefixt" door tijdsbudgetten verder op te rekken zonder aanwijzing dat dát de oorzaak is -- in plaats daarvan de job via `gh run rerun --failed` op een verse runner opnieuw gestart. **Bevestigd runnerruis, geen codeprobleem**: de herstart gaf een compleet ander, willekeurig faalpatroon (SKIN-H-016/SKIN-H-014, geen van de 4 eerdere tests), en een tweede herstart daarna was volledig groen. | Onderscheid maken tussen "één specifieke test faalt steeds op dezelfde regel" (onderzoekswaardig, zie R29) en "willekeurige tests falen allemaal op dezelfde generieke infrastructuurstap" (runnerruis, gewoon herstarten) voorkomt dat de laatste categorie tijd kost aan een nutteloze code-jacht. | vast |
| R32 | **Elke Promote-Prod-shard (en de TransIP-deployjob) vroeg zijn EIGEN goedkeuring aan, niet één gedeelde poort per pipeline-run** — de aanname in R30's eigen commentaar ("GitHub bundelt de prod-reviewerpoort per workflow-run, niet per job") bleek feitelijk onjuist, opgemerkt doordat de gebruiker 7+ losse "Deployment review"-mails kreeg voor één pipeline-run. Elke job die `environment: prod` declareert opent GitHub-breed zijn eigen deployment-review-verzoek; met 10 gesharde Promote-Prod-jobs + de deploy-job was dat 10-11 losse goedkeuringsmails per run. Gefixt (`release-pipeline.yml`): één nieuwe, ongesharde `prod-gate`-job draagt als enige nog `environment: prod`; de 10 Promote-Prod-shards en `deploy-prod` hangen er via `needs: [..., prod-gate]` van af zonder zelf een omgeving te declareren, en lopen na één klik op "Approve" automatisch door. | Rechtstreekse gebruikersfeedback op een concrete schermafbeelding, niet zelf gevonden tijdens CI-monitoring — een herinnering dat de eigen aannames in eerdere commentaarregels (R30) soms nooit echt getoetst waren tegen de daadwerkelijke GitHub-mailflow. | vast |
| R33 | **Live TEST-feedback (tester Shawn-Douglas, 10 sep): tab-navigatie tussen uren-invoervelden in Nieuw sprong steeds terug naar het begin van de week** in plaats van door te gaan naar de volgende dag. Oorzaak: `handleBentoDayCardChange()` (`assets/app.js`) herbouwt bij elke waardewijziging via `rerenderActiveTimesheetView()` de volledige `#new-bento-days`/`#hours-grid-cards`-innerHTML — ook het element waar de browser via Tab net naartoe onderweg was. Doordat "change" bij het verlaten van een veld vuurt vóórdat die toetsenbordnavigatie is afgerond, bestond de doelnode niet meer op het moment dat Tab 'm probeerde te focussen, en viel de focus terug op `<body>` — vanaf daar telt de volgende Tab weer vooraan de pagina. De opgeslagen waarde zelf was altijd goed; alleen de focus na de herbouw ging verloren. Gefixt door na de herbouw te controleren of de focus daadwerkelijk op `<body>` viel, en zo ja expliciet het volgende dagveld (of anders hetzelfde) te focussen. Regressie `[SKIN-H-026]`: eerst bevestigd dat hij zonder de fix faalt (`git stash`), dan dat hij ermee slaagt. | Dezelfde onderliggende klasse als R24/R28 dit sessie: een volledige DOM-herbouw op elk klein statewijziging is goedkoop om te schrijven maar kan de browser's eigen toetsenbord-/scrollstate breken op manieren die alleen bij daadwerkelijk toetsenbordgebruik zichtbaar worden — een muis-only testpad mist dit soort bugs stelselmatig. | vast |
| R34 | **Live TEST-ontwerpfeedback (tester Shawn-Douglas, 10 sep) opgepakt, deel 1 van 3: tekstredundantie op de klanturenstaat-schermen ingekort.** (1) De "Nog aanleveren"-kaart op het Nieuw-dashboard herhaalde in de beschrijving letterlijk wat de badge+titel al zeiden ("De klanturenstaat staat nog open en blokkeert de factuurafronding totdat hij is aangeleverd of als rechtstreeks gemaild is geregistreerd." → "Blokkeert de factuurafronding totdat je 'm aanlevert of als rechtstreeks gemaild meldt."). (2) De 3-stappen-instructielijst ("Kies de maand" / "Kies het bestand" / "Bewaar of dien in") op het klanturenstaat-uploadscherm legde uit wat de velden en knoppen er direct onder al zelfverklarend tonen — vervangen door één korte inleidende zin. Geen tests raakten deze exacte teksten (gecontroleerd via grep vóór wijzigen); `customer-timesheet-api.spec.ts` (18/18) en `[SKIN-H-018]` lokaal opnieuw groen. **Deel 2/3 (voortgangsring + 4-stappenblok-ruimtegebruik in Nieuw) bewust nog niet aangepakt**: dat is een layoutkeuze (bv. wat te tonen zodra een maand al 100% klaar is) die visuele iteratie verdient, geen blinde CSS-tweak. | Tekstredundantie is objectief inkortbaar zonder functieverlies; layout-"ruimtegebruik" is een smaakoordeel dat om screenshots/overleg vraagt voor er iets verandert — bewust niet over één kam geschoren. | vast (deel 1/3) |
| R30 | **CI-shards van 8 naar 10 op Validate/Promote Test/Promote Prod** (`release-pipeline.yml`), na een échte CI-timeout: run `34479315046` (v1.0.56), Validate-shard 7 werd na 13m54s (98/100 tests klaar) hard afgekapt door de `timeout --signal=TERM --kill-after=30s 14m`-wrapper (R4), niet omdat een test vastliep maar omdat shard 7 gewoon meer testtijd nodig had dan de vaste 14 minuten toelieten. Directe oorzaak: deze sessies eigen mobiele dekkingsuitbreiding (R25/R26, dashboard.spec.ts/help-widget.spec.ts + 5 verdere bestanden nu ook op mobile-chrome/mobile-safari) verhoogde het totale aantal tests aanzienlijk zonder de shardverdeling aan te passen. Alle drie de matrix-blokken (Validate, Promote Test, Promote Prod) en hun `--shard=X/N`-denominators tegelijk bijgewerkt voor consistentie; de 14m-veiligheidslimiet zelf bewust ongemoeid gelaten (die vangt nog steeds een écht vastgelopen browserproces snel af, R4's oorspronkelijke doel). | Sharding-aantal is geen eenmalige instelling maar moet meegroeien met de testsuite; dit is de tweede keer (na R4's 4→8) dat testdekkingsgroei de vaste tijdslimiet raakte. | vast |
